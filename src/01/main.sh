#!/bin/bash

if [ $# -ne 1 ]; then
    echo "Пожалуйста, укажите один аргумент."
    exit 1
fi

if [[ $1 =~ ^[0-9]+$ ]]; then
    echo "Ошибка: Введено число, а ожидался текстовый параметр."
else
    echo "Введенный параметр: $1"
fi
